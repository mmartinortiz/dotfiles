When you need web search, prefer `ddg` MCP tools first (`ddg_search`, `ddg_fetch_content`).

Fallback: if `ddg` blocked/rate-limited, use built-in `websearch` / `webfetch`.

Default DuckDuckGo settings:
- SafeSearch: STRICT
- Region: uk-en
